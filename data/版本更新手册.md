# bidking-booooot 数据版本更新手册

游戏客户端或配置表仓库更新后，按本手册刷新 `data/` 下与定价、掉落权重相关的静态资源。

**工作目录**：`D:\workzone\bidking-booooot`（下文命令均在此目录执行）

**真相源**（任选其一，内容应与当前游戏版本一致）：

- Steam 客户端：`BidKing_Data\StreamingAssets\Tables\`
- 配置表仓库：`D:\workzone\bidking-resource\tables\`

---

## 一、文件清单

### 1. 输入（从游戏拷贝到 `data/`）

| 文件 | 说明 |
|------|------|
| `Item.txt` | 物品主表（Base64 Tab） |
| `Drop.txt` | 掉落图 |
| `RankMap.txt` | 地图排位 / 子图配置 |
| `Skill.txt` | 技能表（技能有变动时） |

```cmd
copy /Y D:\workzone\bidking-resource\tables\Item.txt data\
copy /Y D:\workzone\bidking-resource\tables\Drop.txt data\
copy /Y D:\workzone\bidking-resource\tables\RankMap.txt data\
copy /Y D:\workzone\bidking-resource\tables\Skill.txt data\
```

### 2. 工具生成（跑命令产出）

| 输出文件 | 生成命令 |
|----------|----------|
| `item_prices.csv` | `bidking-item-table` |
| `drop_table_weights.csv` | `bidking-game-tables` |
| `rank_map_export.csv` | `bidking-game-tables` |
| `calculator_data_merged.csv`（DROP 行） | `bidking-game-tables --merge-calculator` |
| `Skill_export.csv` | `bidking-skill-table` |
| `map_quality_avg_out.csv` | `python tools\map_quality_avg_prices.py` |
| `map_quality_p25_out.csv` / `map_quality_p50_out.csv` | `python tools\map_quality_p25_p50_prices.py` |
| `物品轮廓爆率推断器.html`（`NEST_W` / `SUBMAP_PRIOR_MULT`） | `python tools\update_infer_html_embed.py` |

### 3. 可选 / 手工维护

| 文件 | 说明 |
|------|------|
| `物品轮廓爆率推断器.html`（`EMBED_CSV.rate` 等） | 爆率表若独立变更需手工；`--sync-embed-item` 可同步物品内嵌 CSV |
| `calculator_data_merged.csv`（ITEM 行） | `--merge-calculator` 只重写 DROP，ITEM 行保留 |
| `skill_export_generated.py` | 本地 `build\generate_skill_export_table.py`（`build/` 未入库） |
| `skill_parsing_report.csv` | 本地 `build\generate_skill_parsing_report.py` |

### 4. 无需随版本重生成

| 文件 | 说明 |
|------|------|
| `game_match_reports.csv` | 对局日志累积 |
| `self_bid_cache.json` | 运行期出价缓存 |
| `emoji_signal_public_blacklist.csv` | 腾讯文档黑名单，独立同步 |

---

## 二、首次准备

```cmd
cd D:\workzone\bidking-booooot
pip install -e .
```

未 `pip install` 时，每条命令前加：

```cmd
set PYTHONPATH=src
```

并用 `python -m bidking.tools.<模块名>` 代替 CLI 名称。

---

## 三、完整更新流程（推荐）

### 步骤 1：拷贝最新表

见上文「输入」一节。

### 步骤 2：物品价格表

```cmd
bidking-item-table --item-txt data\Item.txt --out data\item_prices.csv
```

可选：display 列为空时指定语言表：

```cmd
bidking-item-table --item-txt data\Item.txt --lang-txt D:\workzone\bidking-resource\tables\Language.txt --out data\item_prices.csv
```

导出规则：`item_id > 1_000_000`，且 `category_tags` 含 101–110（棋盘藏品）、14（皮肤）或 100（特殊档）。

### 步骤 3：掉落与地图表

```cmd
bidking-game-tables --data-dir data --write-decoded --merge-calculator data\calculator_data_merged.csv --merge-out data\calculator_data_merged.csv
```

产出：

- `drop_table_weights.csv`
- `rank_map_export.csv`
- `Drop.decoded.tsv` / `RankMap.decoded.tsv`（调试明文，可删）

### 步骤 4：技能表（技能有变动时）

```cmd
bidking-skill-table --skill-txt data\Skill.txt --out data\Skill_export.csv
```

若本机有 `build\` 脚本，继续：

```cmd
python build\generate_skill_export_table.py
python build\generate_skill_parsing_report.py
```

技能语义变化时还需人工检查 `src\bidking\parsing\skill_bindings.py`。

### 步骤 5：推断器 HTML 巢权重 / 子图池化（影响 bot 定价先验）

在 **`item_prices.csv` 与 `calculator_data_merged.csv`（DROP）已更新后** 执行：

```cmd
python tools\update_infer_html_embed.py
```

可选：同时把 `item_prices.csv` 写入 HTML 内嵌 `EMBED_CSV.item`：

```cmd
python tools\update_infer_html_embed.py --sync-embed-item
```

预览不重写文件：

```cmd
python tools\update_infer_html_embed.py --dry-run
```

**含义简述**

| 常量 | 算法 |
|------|------|
| `NEST_W[nest]` | 巢穴根 `drop_id` 池在品质 1–6 下的条件期望件均价 |
| `SUBMAP_PRIOR_MULT[tier][map][item]` | 该子图池内物品份额 ÷ 同档基础子图（如 2101–2107）均值 |

二者由 `item_db._resolve_drop_to_items` 从 `calculator_data_merged.csv` 展开；bot 启动时从 HTML 读入（`item_db.load_map_prior_data`）。

若新增子图且不在 `item_db.MAP_TO_TIER_NEST` / HTML `SUBMAPS_BY_TIER` 中，需先改代码再重跑本脚本。

### 步骤 6：地图品质均价 / 分位价

依赖：`item_prices.csv`、`calculator_data_merged.csv`；巢权重已写入 HTML 时 bot 定价与推断器一致。

```cmd
python tools\map_quality_avg_prices.py
python tools\map_quality_p25_p50_prices.py
```

`map_quality_avg_prices.py` 默认写入 `data\map_quality_avg_out.csv`；仅预览可加 `--no-write`。

### 步骤 7：校验

```cmd
python -m pytest tests/parsing/test_item_db_maps.py tests/parsing/test_skill_csv_registry_consistency.py tests/tools/test_item_table.py tests/tools/test_game_tables.py -q
```

关注：

- RankMap 新 `map_id` 是否已在 `src\bidking\parsing\item_db.py` 的 `MAP_TO_TIER_NEST` 中登记
- 技能 CSV 与 `skill_bindings` 是否一致

---

## 四、最小更新（仅 Drop / RankMap 变动）

未新增物品、未改技能时：

```cmd
bidking-game-tables --data-dir data --merge-calculator data\calculator_data_merged.csv --merge-out data\calculator_data_merged.csv
python tools\map_quality_avg_prices.py
python tools\map_quality_p25_p50_prices.py
```

---

## 五、可能需要改代码的情况

| 现象 | 处理 |
|------|------|
| `test_item_db_maps` 报 RankMap id 缺失 | 在 `item_db.py` 的 `MAP_TO_TIER_NEST` 补充新地图 → 巢穴映射 |
| 新地图档定价参数 | 检查 / 新增 `configs\pricing.maps\*.json` |
| 新技能 id / 日志字段 | 更新 `Skill_export.csv` 后跑 build 脚本，并改 `skill_bindings.py` |
| 子图池化 / 巢权重变化 | `python tools\update_infer_html_embed.py`（见步骤 5） |

---

## 六、CLI 速查

| 命令 | 作用 |
|------|------|
| `bidking-item-table` | `Item.txt` → `item_prices.csv` |
| `bidking-game-tables` | `Drop.txt` / `RankMap.txt` → CSV |
| `bidking-skill-table` | `Skill.txt` → `Skill_export.csv` |
| `bidking-sync-blacklist-sheet` | 拉取腾讯文档 → 表情黑名单 CSV |
| `python tools\update_infer_html_embed.py` | 重算 HTML 内 `NEST_W`、`SUBMAP_PRIOR_MULT` |

---

## 七、依赖关系简图

```
Item.txt ──────────────► item_prices.csv ──┐
                                           ├──► map_quality_*_out.csv
Drop.txt ──┬──► drop_table_weights.csv ───┤
           └──► calculator_data_merged.csv ┘
RankMap.txt ───────────► rank_map_export.csv
Skill.txt ─────────────► Skill_export.csv ──► skill_export_generated.py（本地 build）

update_infer_html_embed.py ──► 物品轮廓爆率推断器.html（NEST_W / SUBMAP_PRIOR_MULT）
                                      └──► item_db 巢权重 / 子图先验（bot 定价）
```

---

## 八、打包分发提醒

`dist\grid_view.exe` **不包含** `data/`。发给别人时需一并提供更新后的 `data\` 与 `configs\`，或将 exe 放在含完整 `data\`、`configs\` 的项目根下运行。

---

*最后更新：2026-06-11（含 `bidking-item-table`）*
> python -m bidking.runner.viewer_main   